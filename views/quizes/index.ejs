<div><table>
<% var i;for (i=0; i< quizes.length; i++){ %>
  <tr><td><a href="quizes/<%= quizes[i].id %>"><%= quizes[i].pregunta %> ( Tema: <%= quizes[i].tema %> )</a></td>
    <% if(session.user){%>
      <td><a href="quizes/<%= quizes[i].id %>/edit"><button>editar</button></a></td>
<td><form method="post" action="quizes/<%= quizes[i].id %>?_method=delete">
          <button type="submit" onClick="return confirm('Borrar: <%= quizes[i].pregunta %>');">
            borrar
          </button>
        </form>
      </td>
    <%}%>
</tr>
<% } %>
</table></div>
<p>
<% if(session.user){%>
  <a href="/quizes/new"><button type="button">Crear pregunta</button></a>
  <%}%>
<div>
<!-- Para el ejercicio final del modulo 7 inserto el formulario para filtrar
las preguntas -->

<form method="get" action="/quizes">
Filtro preguntas<p>
Tema:
<select name="tema">
  <option value="otro" >Otro</option>
  <option value="humanidades">Humanidades</option>
  <option value="ocio">Ocio</option>
  <option value="ciencia">Ciencia</option>
  <option value="tecnologia">Tecnología</option>
</select>
<p>
Texto a buscar:
  <input type="text" name="search" value=""><p>
  <input type="submit" value="Enviar">
</form>
</div>
